name: Sell Logic Guard (DISABLED)

on:
  # Manual only (prevents auto runs)
  workflow_dispatch:

jobs:
  verify-sell-logic:
    if: ${{ false }}   # hard-disable legacy checker
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy Sell Logic Guard is disabled for the Hourly 1-Coin Rotation strategy."
